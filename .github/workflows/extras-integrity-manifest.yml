--- a/.github/workflows/extras-integrity-manifest.yml
+++ b/.github/workflows/extras-integrity-manifest.yml
@@ -3,7 +3,6 @@ name: Extras SHA-256 Manifest Integrity
 on:
   push:
     branches: [ main ]
     paths:
       - 'bash4llm'
-      - 'extras/**'
       - '.github/workflows/extras-integrity-manifest.yml'
   pull_request:
     branches: [ main ]
@@ -45,7 +44,10 @@ jobs:
             rm -f "$TMP_CLEAN_MANIFEST"
             
             # Verify native bash4llm extras installer integrity test safely
-            INSTALL_LOG=$("$B4L_BIN" --install-extras 2>&1 || true)
+            INSTALL_LOG=$("$B4L_BIN" --install-extras 2>&1) || {
+              echo "❌ ERROR: Extras installer failed with exit code $?!"
+              exit 1
+            }
             echo "$INSTALL_LOG"
             
             if echo "$INSTALL_LOG" | grep -qi "integrity verification warnings"; then
